{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "First large scale project made with help",
  "license": "ISC",
  "author": "alAyaan",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start":"npm run start --prefix backend"
  }
}
